[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "swe-onboarding-tickets"
version = "0.1.0"
description = "Support ticket CRUD microservice"
requires-python = ">=3.12"
